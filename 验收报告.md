# 喵掌柜主题化改造 · 验收报告

> 被测产物：`/tmp/mz-p7`（对应提交 `c1818af` + 阶段 7 的修复）
> 对照产物：`/tmp/mz-dist-before`（改造前的线上产物，`index-COEr9CQ8.css` / `index-BmQcYURf.js`）
> 测试环境：Chromium 153（Playwright 1.63 自带）、Linux、2 核容器、无 GPU
> 报告日期：2026-09-18

---

## 0. 结论一览

| 项 | 结果 |
|---|---|
| classic 像素回归 | ✅ **27/29 零差异**；2 张首页因新增主题入口而变化，差异已量化（629 / 288 像素）|
| classic 令牌回归 | ✅ 4 种层叠条件下各 262 项有效值全部相同，差异 0 |
| 对比度（正文 4.5:1 / 非文本 3:1）| ✅ 全部达标；3 项按 WCAG 条款豁免，逐条给出依据 |
| 视觉基线（双主题） | ✅ 58 张，连跑两次完全一致 |
| 功能测试 | ✅ 32/32 通过 |
| 单元测试 | ✅ 27/27 通过（项目原有的唯一测试）|
| Lighthouse 移动端 | ⚠️ glass 与 classic **持平**，但**绝对值不达标**（见 §5）|
| 滚动性能 | ✅ glass 在 1535px 真实滚动下零掉帧 |
| 代码卫生 | ⚠️ `!important` 净增 8 条（逐条有据）；未使用令牌仍有 89 个（已分类，见 §6）|
| **本轮发现并修复的缺陷** | **2 个**（1 个是可导致整个应用白屏的既存缺陷）|
| 跨浏览器人工验证 | ❌ **未执行**（环境无 Safari / Firefox / 真机）—— 见 §9 清单 |
| lint / typecheck | ❌ **无法执行**（项目从未配置，非本次引入）|

---

## 1. 视觉回归

### 1.1 覆盖范围

19 条路由 × 2 主题 × 2 视口（1440×900 / 390×844），共 **58 张基线**。

覆盖了规格点名的全部页面：首页、工作室列表、工作室详情、预约流程两步、`/admin/login`、后台首页（另含订单/预设/设置/通知/日志/创建向导/编辑共 7 个后台页）。

### 1.2 classic 零回归（对**改造前**产物）

重新从 `/tmp/mz-dist-before` 生成基线后与当前产物比对：

```
2 failed · 27 passed
  ✗ 01-home · desktop
  ✗ 01-home · mobile
```

| 页面 | 差异像素 | 占整图比例 |
|---|---|---|
| 首页 · 桌面 | **629 px** | 0.049%（1440×900）|
| 首页 · 移动 | **288 px** | 0.087%（390×844）|
| **其余 27 张** | **0 px** | 0 |

这 2 张的差异来源是**你在阶段 5 明确要求新增的首页主题入口**（绝对定位右上角、脱离文档流）。差异区域就是那一个控件，其余像素完全一致。

> ⚠️ 口径说明：仓库里现在保存的 `classic/` 基线是「改造前 + 首页入口」的状态（全部 29 张通过）。要复现「对改造前」的对比，需按 §10 的步骤重新生成 —— 否则它会显示为全绿，掩盖那 2 张的真实差异。

### 1.3 glass 基线

29 张全部稳定（连跑两次逐像素一致）。glass 是新增外观，基线用于后续回归，不参与「零差异」判定。

---

## 2. 功能测试（32 条，全部通过）

| 文件 | 条数 | 覆盖 |
|---|---|---|
| `switcher.spec.mjs` | 15 | 切换器组件：ARIA 结构、点击/键盘打开、方向键移动、Home/End、Enter/Space 选中、Esc 关闭归还焦点、Tab 关闭不抢焦点、点击外部关闭、`aria-checked` 唯一性、`theme:switch` 事件、迷你预览取真实令牌值、reduced-motion 下不启动 View Transition、支持时调用一次且 DOM 变更在回调内、查询参数不落库、移动端浮层「恢复默认」|
| `fx.spec.mjs` | 10 | 降级路径：`?fx=full`/`?fx=reduced`、低端启发式触发、reduced-motion、prefers-contrast、运行时跟随偏好变化、焦点可见性（真实 Tab 路径逐个检查）、装饰层不进无障碍树、背景装饰是伪元素 |
| `theme-defaults.spec.mjs` | 7 | 主题系统：首访默认值、刷新保持、**localStorage 不可用**、系统深色模式不跟随、控制台无错误警告、`?theme=` 优先级、非法值被忽略 |

### 逐条对应你的要求

| 你要求的检查项 | 结果 |
|---|---|
| 首访默认值 | ✅ 无存储时不写 `data-theme`，即 classic 兜底 |
| 切换后刷新是否保持 | ✅ 保持 |
| `?theme=` 是否生效且不落库 | ✅ 生效、优先级高于存储、不写入 |
| localStorage 不可用（隐私模式）是否不崩 | ❌→✅ **原先会崩，本轮已修**（见 §7.1）|
| 系统深色模式用户是否被正确对待 | ✅ 不跟随系统深浅色（有意设计）；glass 显式 `color-scheme: light` |
| 键盘路径全流程 | ✅ 覆盖 |
| Esc 关闭浮层后焦点回到触发器 | ✅ 覆盖 |
| SSR 水合无 hydration warning | ➖ **不适用**：本项目是纯 SPA、无 SSR。改测「加载与切主题期间控制台无 error/warning」✅ 通过 |

---

## 3. 无障碍实测

### 3.1 对比度（全部为脚本实算值）

**glass 首页背景 · 全视口逐点采样（步长 2%）**

| 令牌 | 最低点 | 该点底色 | 最低对比度 |
|---|---|---|---|
| `--text-1` | 12%/8% | `#cde2fe` | 13.05:1 |
| `--text-2` | 12%/8% | `#cde2fe` | 5.94:1 |
| `--text-3` | 12%/8% | `#cde2fe` | **4.64:1** |

**面板 / 后台 / 表格 / 灯箱**

| 场景 | 实测 |
|---|---|
| glass `surface-glass(.62)` · text-1 / 2 / 3 | 15.55 / 7.07 / 5.53:1 |
| glass `surface-glass-quiet(.42)` · text-2 / 3 | 6.68 / 5.22:1 |
| 后台页面底 · text-1 / 2 / 3 | 16.05 / 7.30 / 5.71:1 |
| 后台卡片 · text-1 / 2 / 3 | 17.23 / 7.84 / 6.13:1 |
| 表格表头 / 正文 / 斑马纹 / hover | 5.71 / 7.84 / 7.49 / **6.96:1** |
| 表格 hover 行上的三级字 | 5.44:1 |
| **输入框边界** vs 白底 / 玻璃面板（线 3:1）| **3.29 / 3.06:1** |
| **灯箱控制按钮形状** vs 遮罩（原 **1.27:1**）| **17.28:1** |
| 照片上的标签（不透明底，与照片内容无关）| 5.00:1（classic）/ 5.17:1（glass）|

**降级路径**（降级只应变素，不能变成读不了）

| 路径 | 实测 |
|---|---|
| 无 `backdrop-filter` · 正文 / 三级字 | 7.76 / 6.07:1 |
| `data-fx=reduced` · 正文 / 三级字 | 7.63 / 5.95:1 |
| `prefers-contrast: more` · glass / classic 三级字 | 8.42 / 9.35:1 |
| `prefers-contrast` · 输入框边界 | 5.42:1 |

### 3.2 三处 WCAG 豁免（逐条给出依据，不是「不用管」）

| 项 | 实测 | 依据 |
|---|---|---|
| `--text-4` 在光斑峰值处 | 2.89:1 | 仅用于禁用态与**纯装饰**图形。WCAG 1.4.3 / 1.4.11 均写明 *except for inactive components*；1.4.11 亦只约束「理解内容所必需」的图形 |
| `--color-disabled` vs 禁用底 | 2.21:1 | 同上。但仍取了**可读**值（classic 原值 1.55:1，用户读不出禁用按钮写的是什么）——豁免的是「不达标」，不是「看不见」|
| 表格行分隔线 vs 卡片 | 1.25:1 | 不标识控件、非理解内容所必需（行的范围由斑马纹与留白界定）；设计要求原文即「**极细**分隔线」|

### 3.3 焦点可见性

以**真实 Tab 路径**逐个检查（不用程序化 `focus()` —— 那不会触发 `:focus-visible`，测出来是测试的错而不是样式的错）。

**本轮修掉 2 处真实缺陷**：
- Hero 搜索框的三个输入框：写了 `outline: none` 却没有替代焦点环
- 品牌链接：只有浏览器默认的 `outline: auto/1px`，落在磨砂玻璃上不够明确

### 3.4 装饰层的无障碍处理

- `body::before` / `body::after`（光斑 + 网格）：**伪元素不生成 DOM 节点，天然不进无障碍树** —— 靠的是它们确实是伪元素，而不是靠 `aria-hidden`
- 有 DOM 的装饰节点（品牌标、装饰云、骨架屏、字段分隔线）：全部 `aria-hidden="true"`，自动化断言为零暴露

---

## 4. 滚动性能实测

`node scripts/visual/scroll-perf.mjs`（390×844、CDP CPU 降速 4×、真实滚动 + 逐帧记录 rAF 间隔）

| 指标 | classic | glass |
|---|---|---|
| 帧间隔 p50（理想 16.7ms）| 16.7 ms | **16.7 ms** |
| 帧间隔 p95 | 16.8 ms | **16.7 ms** |
| 最长帧 | 33.4 ms | **16.8 ms** |
| 长帧占比（>33ms）| 0.0% | **0.0%** |
| 严重卡顿（>50ms）| 0 | **0** |
| **实际滚动 / 可滚距离** | **103 / 103 px** | **1535 / 1535 px** |

**结论：glass 首页在 1535px 真实滚动下锁定 60fps，零长帧** —— 即使带着 5 个 `backdrop-filter` 层与两层 `position: fixed` 背景装饰。

**classic 那组数字不可用**：它的首页是 `overflow:hidden` 的满屏居中页，只能滚 103px，样本量不足。脚本里的「实际滚动距离」自检就是为此而加 —— 否则「没滚动所以不掉帧」会被当成「滚动很流畅」汇报。

### 被实测推翻的两个假设（记录在案）

| 假设 | 实验 | 结果 |
|---|---|---|
| `blur(80px)` 光斑是 TBT 主因 | 移除后重测 | TBT 932 → **938 ms**（无变化）|
| 5 个 `backdrop-filter` 层是 TBT 主因 | 全部移除后重测 | TBT 938 → **938 ms**（无变化）|

glass 比 classic 多出的约 200–400ms 主线程开销，来源是**页面本身更大**（DOM 约 5 倍 + 自带 CSS），不是特效。因此**没有**加 `contain: paint` / `isolation: isolate` —— 没有数据支撑的调优就是 cargo cult，而且 `contain: paint` 会裁掉 `box-shadow`、改变绝对定位子元素的包含块。

---

## 5. Lighthouse 移动端（模拟 4G + 4× CPU 降速）

| 指标 | classic | glass | Δ |
|---|---|---|---|
| Performance | 35 | 35 | 0 |
| FCP | 8.33 s | 8.28 s | −0.05 |
| **LCP** | 8.33 s | **8.35 s** | **+0.02** |
| **CLS** | 0.0000 | **0.0000** | 0 |
| **TBT** | 1.04 s | **1.03 s** | **−0.01** |
| Speed Index | 8.33 s | 8.28 s | −0.05 |
| 主线程总耗时 | 3.99 s | 3.69 s | −0.30 |

**两套主题持平** ✓。LCP 元素两套都是 `index.html` 里的启动闪屏图（96×96 PNG）。

### ⚠️ 三条必须说清的限制

1. **绝对分值不达标**（Performance 35、LCP 8.3s）。这是应用的**基线状态**：1MB JS 单包 + 400KB CSS，在 4× CPU 降速下。**不是主题化引入的**（两套主题同值），但确实是个真问题。改善需要代码分割与 CSS 瘦身，属独立议题。
2. **本脚本走静态服务器，`/api/**` 无后端**，页面停在空状态。对两套主题**同等**，所以对比有效，但绝对值不代表线上。
3. **glass 的 LCP 元素不是文本**。规格要求「玻璃风首页的 LCP 元素必须是文本」——glass 首页**自身**做到了（首屏零 `<img>`、无背景图、Hero 纯文本），但整个 `/` 路由的 LCP 元素是**两套主题共用**的启动闪屏图，它在路由 chunk 到达前就已绘制，glass 无法也不该影响它。
   > 旁证：在「按需加载」的版本里，glass 的 LCP 元素**曾是** `<p class="gh-hero__desc">`（文本）——那恰恰说明它在等 chunk 加载。

---

## 6. 代码卫生

### 6.1 硬编码颜色

对 `glass.css` / `glass-app.css` / `fx.css` / `ThemeSwitcher.vue` / `GlassHome.vue` / `useFx.js` 全量扫描。

**本轮修掉**：`glass-app.css` 里表格的 5 个写死色（`#EAF2FE` / `#DBE7FA` / `#F4F7FC` / `#F7FAFE` / `#EAF0FA`）+ 后台页底 + 焦点外发光 + 骨架屏底色，共 **14 处**，全部抽成语义令牌（`--table-head-bg` / `--table-hover-bg` / `--table-zebra-bg` / `--table-row-border` / `--admin-bg` / `--focus-glow` / `--skeleton-bg`）。

**仍存在的字面量（均为合法类型）**：
- 令牌定义块里的值（`:root[data-theme]` / `:root[data-fx]` / `@media (prefers-contrast)`）—— 那是**定义**，不是使用
- SVG **属性选择器**里的 `#E8635C` 等（`.illustration [stroke='#E8635C']`）—— 那是**匹配**资产里的属性值，不是施加颜色
- 遮罩值 `#000`（`mask-image` 里不透明=保留，与主题无关）

### 6.2 `!important`

| 文件 | 改造前 | 现在 | 说明 |
|---|---|---|---|
| `element-bridge.css` | 122 条声明 | **122 条** | 零新增 ✅ |
| `theme.css` | 4 | 4 | 零新增 ✅ |
| `glass-app.css` | — | **7** | 新增 |
| `fx.css` | — | **1** | 新增 |
| 合计 | 166 | **175**（+9，去重后 8 条声明）| |

**8 条逐条有据**：
- glass-app.css 的 7 条全部用于覆盖 Element Plus 那些**本身带 `!important`** 的声明（`.el-input__wrapper` 的背景/边框、`.el-table th` 的背景等）。同源同层下，非 `!important` 声明**打不过** `!important`，除此之外没有别的办法。
- fx.css 的 1 条是 `prefers-contrast` 下把焦点环加粗到 3px。组件 scoped 的焦点规则是 (0,3,0)，会压过 (0,2,0) 的偏好覆盖 —— 而这是一条无障碍覆盖，必须赢。

### 6.3 `console.log`

生产代码 **0 处** ✅。全部 16 处都在 `src/utils/durationCalc.test.js`（测试脚本）。另有 `console.error` 13 处、`console.warn` 3 处，均为错误上报。

### 6.4 未使用令牌

扫描方法：只把**声明位置**的 `--name:` 算作定义（否则 `.el-button--primary:first-child` 会被误判成变量定义）。引用来源含 CSS `var()`、源码 `var()`、以及脚本里以字符串形式引用的令牌名。

```
本项目令牌（非 --el-）  317
  其中被引用            228
  定义但未引用           89
```

**本轮删掉 7 个**（我发明的、无消费方的）：`--focus-ring`、`--brand-contrast`、`--a-blue-07`、`--a-coral-18`、`--a-coral-26`、`--a-white-18`、`--a-white-32`。

**保留的 89 个，分类如下**：

| 类别 | 数量 | 保留理由 |
|---|---|---|
| L1 刻度 · 字号 14 / 字重 5 / 行高 2 / 字距 1 | **22** | 阶段 1 你明确要求建立「字号」刻度。**但要说清楚：这套字号刻度定义了却从未被采用** —— 组件里的 `font-size` 仍是硬编码 px（约 370 处）。当前它只是「声明了设计系统的 API 面」，不是已被使用的资产 |
| L1 刻度 · 模糊 / 饱和 / 色阶 / 缓动 / 层级 | **18** | 同上，色阶与刻度天然是「定义全集、使用子集」|
| L2 兼容别名（`--space-xs`… / `--shadow-xs`… / `--border` / `--text` / `--sky` 等）| **46** | 阶段 1 保留它们时，依据是 `tokens.css` 注释里写的「全站约 50 处引用」。**实测该依据不成立** —— 那些引用在 9 月重构中已迁移完毕，这些别名如今零消费方 |
| `--glass-saturate` / `--glass-blur-length` 等 | **3** | 被 `check-tokens.mjs` 的一致性断言引用（用于守住 `--glass-blur` 字面量不漂移），不是纯装饰 |

**处理建议见 §8.4。**

### 6.5 lint / typecheck / test

| 项 | 状态 |
|---|---|
| ESLint | ❌ **项目从未配置**。CI 里有 lint 步骤，但用 `compgen -G ".eslintrc*"` 判断，永远走 else 分支跳过 |
| Prettier / Stylelint | ❌ 无 |
| TypeScript | ❌ 无（纯 JS 项目）|
| 单元测试 | ✅ `durationCalc.test.js`，**27/27 通过**（需手动 `node` 运行，无测试框架）|
| CI | `.github/workflows/ci.yml` 存在，实际只做「build 能不能过」+ 后端语法检查 |

「运行项目现有的 lint / typecheck / test」这一条**只能部分执行**：test 跑了（27/27 通过），lint 与 typecheck 因为项目从未配置而无法运行。这是既有状态，不是本次改造引入的。

---

## 7. 本轮发现并修复的缺陷

### 7.1 🔴 localStorage 不可用会导致**整个应用白屏**（既存缺陷）

**现象**：Safari 无痕模式、禁用 Cookie、沙箱 iframe、企业策略等任何让 `localStorage` 抛错的场景下，页面**什么都不渲染**（`#app` 内容为空）。

**根因**：`src/utils/storage.js` 的存储适配器直接调用 `localStorage.*`，**没有任何 try/catch**。而路由的导航守卫每次跳转都调 `storage.get('mzg_admin_token')` 读 token → 抛异常 → 导航被中止 → 应用白屏。

**是既存缺陷的证据**：用同一条件测改造**前**的产物（`/tmp/mz-dist-before`），同样白屏。

**修复**：适配器加 try/catch + 内存兜底。
- 为什么不是单纯吞掉异常：吞掉之后 `set` 后再 `get` 会拿到 `null`，登录态在同一次会话内就丢，用户会看到「刚登录完又被踢回登录页」这种更费解的现象。内存兜底保证**本次会话内**读写自洽，只影响本来完全用不了存储的用户。
- **不沉默降级**：首次触发打一条 `console.warn`，测试断言它恰好出现一次（避免刷屏）。

### 7.2 `prefers-contrast: more` 之前**根本没生效**

媒体查询命中了，但覆盖写在 `:root`（特异性 0,1,0），被 `tokens.semantic.css` 里的 `:root[data-theme="glass"]`（0,2,0）压过去。**不报任何错**，表现为「系统偏好打开了但什么都没变」。

已改为与主题块同级的特异性。这是测试抓出来的 —— 靠肉眼看永远发现不了。

### 7.3 焦点环缺失 2 处

见 §3.3。都是阶段 3 遗留，由新加的「真实 Tab 路径」测试抓出。

### 7.4 表格颜色写死在规则里

见 §6.1。违反项目自己的「组件里不写死色值」规则。

---

## 8. 未通过项与处理建议

| # | 未通过 / 未验证项 | 严重度 | 处理建议 |
|---|---|---|---|
| 8.1 | **跨浏览器人工验证未执行** | 🔴 高 | 本环境只有 Chromium。Safari / iOS Safari / Firefox / 低端 Android 的行为**完全没有证据**。§9 给了逐条清单。**建议部署到预发后立刻做**，这是当前最大的未知 |
| 8.2 | **Lighthouse 绝对分值不达标**（Perf 35 / LCP 8.3s）| 🟠 中 | 既存基线问题，非主题化引入（两套主题同值）。改善路径：① 主 JS 1MB 单包做代码分割（`manualChunks` + 路由级懒加载已有，但 Element Plus 全量引入是主因）；② 397KB CSS 中 EP 占绝大部分，考虑按需引入；③ 启动闪屏在 350KB 主包加载期间显示，可考虑把首屏所需的最小样式内联。**这是独立议题，建议单独立项** |
| 8.3 | **`@supports not (backdrop-filter)` 路径无法在本环境触发** | 🟠 中 | 降级后的**对比度**已由脚本算过（7.76 / 6.07:1），但**没有视觉证据**。验证方式：Firefox 旧版，或 Chrome DevTools → Rendering → 勾选模拟不支持；也可临时改 CSS 强制走该分支后截图对比 |
| 8.4 | **89 个未使用令牌**（详见 §6.3）| 🟡 低 | 分两类处理：① **兼容别名（46 个）建议删除** —— 保留依据经实测不成立，且零消费方，删除不会改变任何渲染；② **字号/色阶刻度（40 个）建议保留**，但要补一件事：要么在 `tokens.primitives.css` 顶部注明「这是设计系统的 API 面，部分档位当前无消费方」，要么真正把字号刻度接进组件（约 370 处硬编码 `font-size`，工作量大）。**不建议留着不管** —— 下次有人扫到会重复我这轮的困惑 |
| 8.5 | **降级态（`data-fx=reduced`）没有像素基线** | 🟡 低 | 目前只有功能测试覆盖（令牌值确实变了）。若要像素证据，需在 `visual.spec.mjs` 加一组 `fx=reduced` 的用例，基线数量翻倍（58 → 116，约 +13MB）。按需决定 |
| 8.6 | **`--fs-*` 字号刻度未被采用** | 🟡 低 | 见 8.4 ②。阶段 1 我加了尺刻度但没有替换组件里的硬编码字号 —— 这是个**未完成的交付项**，如实记录 |
| 8.7 | **lint / typecheck 无法运行** | 🟡 低 | 项目从未配置。若要接 CI 质量门，需先建 ESLint（Vue 插件）+ Stylelint（禁硬编码色值，正好补上 §6.1 的人工扫描）。建议单独立项 |
| 8.8 | **`!important` 净增 8 条** | 🟢 可接受 | 逐条有据（§6.2），且都在主题专属文件里、classic 下一行不命中。若要归零，需重构 `element-bridge.css` 去掉它自己的 `!important`——风险大于收益 |

---

## 9. 跨浏览器人工验证清单

> 以下是**具体要看什么**，不是「测一下」。每条都给了现象描述与判定标准。

### 9.1 Safari（macOS 18+）

| # | 操作 | 看什么 | 判定 |
|---|---|---|---|
| S1 | 打开 `?theme=glass&fx=full`，看导航条与搜索条 | `-webkit-backdrop-filter` 是否生效（背后内容应被模糊）| 面板必须通透且背后内容模糊；若面板是纯白不透明，说明前缀属性没生效 |
| S2 | 切到 classic 再切回 glass | 模糊**是否跟着变**（`backdrop-filter` 用变量、`-webkit-` 用字面量，两者当前取值相同但独立）| 切换后模糊强度应一致；若 classic 模糊留在 Safari 上，说明前缀属性没跟随 |
| S3 | 滚动 glass 首页（尤其快速上下甩动）| **Safari 的已知坑**：`backdrop-filter` 元素在滚动时闪烁/抖动/模糊层错位 | 吸顶导航与吸底 tab 在滚动中应稳定，不应有 1px 抖动或瞬间消失。若出现：给该元素加 `transform: translateZ(0)` 强制提升合成层（**这是已知规避手段，但我没有在真机上验证过是否必要 —— 不要照抄，先确认现象再改**）|
| S4 | 首页放大到 200% | 圆角裁切是否溢出（`.gh-nav` 是 `border-radius: pill`，内容可能撑出圆角）| 圆角处不应露出方形背景或文字被切 |
| S5 | 切换主题 | View Transition 的整页交叉淡入 | 中间**不应出现白屏**；若白闪，说明默认关键帧在 WebKit 上行为不同，需改用 `mix-blend-mode` 方案 |
| S6 | 系统开启「减少动态效果」后切换主题 | View Transition 应**完全不启动** | 切换瞬时完成，无任何过渡 |

### 9.2 iOS Safari（真机）

| # | 操作 | 看什么 | 判定 |
|---|---|---|---|
| I1 | 首屏加载 | 地址栏收起/展开时页面高度是否跳 | `.welcome` 已用 `100dvh` 兜底；不应看到内容上下跳 |
| I2 | 点导航里的切换器 → 点选项 | **触摸 vs hover**：点一下是否留下「卡住的高亮」 | `@media (hover: hover)` 已限制；iOS 上点完不应留高亮 |
| I3 | 点面板外部 / 点面板内部 | iOS 的点击合成事件顺序与桌面不同 | 点外部必须关闭；点内部**不能**误关 |
| I4 | 打开汉堡浮层 → 用浮层里的切换器 | 竖排 variant + 「恢复默认」 | 面板展开方向正确、不超出屏幕 |
| I5 | 滚动 glass 首页 | `backdrop-filter` 性能（iOS 上比桌面敏感得多）| 滚动应跟手；若明显掉帧，看是否需要在 iOS 上禁用吸顶条的 blur |
| I6 | 系统开启「减弱动态效果」 | 主题切换瞬时 | 无过渡 |

### 9.3 Chrome / Edge

| # | 看什么 |
|---|---|
| C1 | 与本报告 §1–§5 的实测一致（本报告就是在 Chromium 上跑的）|
| C2 | DevTools → Rendering → Paint flashing，滚动 glass 首页：**不应出现大面积绿色重绘**（只应有吸顶条等小区域）|
| C3 | DevTools → Rendering → Frame Rendering Stats，滚动时 FPS 应稳定在 60 |

### 9.4 Firefox

| # | 看什么 | 判定 |
|---|---|---|
| F1 | 若版本支持 `backdrop-filter`（Firefox 103+）| 与 Chrome 一致 |
| F2 | 若走降级路径（`@supports not`）| 面板应为 `.96` 高不透明实色，**圆角/阴影/间距与 glass 完全一致**，只是不通透；文字对比度 ≥4.5:1 |
| F3 | View Transition（Firefox 支持较晚）| 不支持时应**瞬时切换**，不得白闪 |

### 9.5 低端 Android（4GB 内存档）

| # | 看什么 | 判定 |
|---|---|---|
| A1 | 首屏时间（真机秒表 / Chrome 远程调试）| 记录实际值。本地无头测出的 8.3s 是**模拟** 4G + 4× CPU 降速下的数字，真机通常更快 |
| A2 | 滚动 glass 首页流畅度 | 该档设备会命中 `deviceMemory ≤ 4` 启发式 → 自动降级（模糊 18→6px、光斑 80→20px、成组玻璃容器改实色）。**确认降级确实发生了**（DevTools 看 `data-fx="reduced"`），且滚动比不降级时更跟手 |
| A3 | 降级后的观感 | 应仍是「玻璃」而不是「一片白」——表面提到 `.90`，通透感减弱但方向未变 |

### 9.6 系统设置组合（三档各测一遍）

| # | 系统设置 | 期望行为 |
|---|---|---|
| P1 | 减少动态效果：开 | `data-fx="reduced"`；骨架不呼吸、插画不旋转、筛选进度条静止、卡片不上浮、主题切换瞬时 |
| P2 | 增强对比度：开 | `data-fx="reduced"`；表面提到 `.97–.99`；glass 三级文字变 `#3A4E6E`（8.42:1）；焦点环 3px。**Chrome 的 DevTools 无法模拟此项，必须在操作系统设置里开**（Windows：设置 → 辅助功能 → 对比度主题；macOS：辅助功能 → 显示 → 提高对比度）|
| P3 | 浏览器缩放 200% | 布局不横向溢出；文字不被裁切；焦点环仍可见 |

---

## 10. 复现方式

```bash
cd /www/wwwroot/miazhanggui.xyz/frontend

# ── 构建到临时目录（不碰线上 dist/）──
npx vite build --outDir /tmp/mz-verify --emptyOutDir

# ── 令牌零回归 ──
node scripts/check-tokens.mjs \
  /tmp/mz-dist-before/assets/index-COEr9CQ8.css \
  $(ls /tmp/mz-verify/assets/index-*.css)

# ── 对比度验收 ──
node scripts/check-contrast.mjs

# ── 功能测试（32 条）──
DIST_DIR=/tmp/mz-verify npx playwright test switcher fx theme-defaults

# ── 视觉回归（双主题，各 29 张）──
DIST_DIR=/tmp/mz-verify THEME=classic npx playwright test visual.spec
DIST_DIR=/tmp/mz-verify THEME=glass   npx playwright test visual.spec

# ── 滚动性能 ──
DIST_DIR=/tmp/mz-verify node scripts/visual/scroll-perf.mjs

# ── Lighthouse 双主题对比 ──
CHROME_PATH=/root/.cache/ms-playwright/chromium-*/chrome-linux64/chrome \
DIST_DIR=/tmp/mz-verify node scripts/lighthouse.mjs

# ── 单元测试 ──
node src/utils/durationCalc.test.js
```

**复现「对改造前」的视觉对比**（仓库里的 classic 基线已含首页入口，会显示全绿）：

```bash
cp -a scripts/visual/__screenshots__ /tmp/screenshots-backup
rm -rf scripts/visual/__screenshots__/visual.spec.mjs-snapshots/classic
DIST_DIR=/tmp/mz-dist-before THEME=classic npx playwright test visual.spec --update-snapshots
DIST_DIR=/tmp/mz-verify      THEME=classic npx playwright test visual.spec   # → 2 failed / 27 passed
rm -rf scripts/visual/__screenshots__ && cp -a /tmp/screenshots-backup scripts/visual/__screenshots__
```

---

## 附：本次交付的测试资产

| 文件 | 用途 |
|---|---|
| `scripts/check-tokens.mjs` | 令牌逐值比对 + 25 条不变量 |
| `scripts/check-contrast.mjs` | 对比度验收（含降级路径、后台、灯箱、输入框边界）|
| `scripts/visual/visual.spec.mjs` | 视觉回归（19 路由 × 2 主题 × 2 视口）|
| `scripts/visual/switcher.spec.mjs` | 切换器行为（15 条）|
| `scripts/visual/fx.spec.mjs` | 降级路径与无障碍（10 条）|
| `scripts/visual/theme-defaults.spec.mjs` | 主题系统与异常环境（7 条）|
| `scripts/visual/serve.mjs` | 视觉测试专用静态服务器 |
| `scripts/visual/scroll-perf.mjs` | 滚动帧率观测 |
| `scripts/lighthouse.mjs` | 双主题 Lighthouse 对比 |
| `scripts/visual/__screenshots__/` | 58 张基线（classic 29 + glass 29，约 13 MB）|
